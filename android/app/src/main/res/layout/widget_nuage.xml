<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="12dp"
    android:background="@drawable/widget_bg">

    <!-- Taille FIXE (pas d'adjustViewBounds) : dans un widget/RemoteViews, adjustViewBounds
         + wrap_content peut déclencher des re-mesures en boucle sur certains launchers,
         d'où un rafraîchissement/clignotement permanent. -->
    <ImageView
        android:id="@+id/widget_nuage"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:scaleType="fitCenter"
        android:src="@drawable/nuage_taquin_radieux"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/widget_temp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="30sp"
        android:textStyle="bold"
        android:textColor="#33414F"
        android:layout_marginTop="2dp"
        android:text="" />

    <TextView
        android:id="@+id/widget_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#33414F"
        android:text="" />

    <TextView
        android:id="@+id/widget_vanne"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textStyle="italic"
        android:textColor="#4A5A6A"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginTop="4dp"
        android:text="" />
</LinearLayout>
