<?xml version="1.0" encoding="utf-8"?>
<!-- Disposition « compacte » (petit carré ~2×2) : nuage + température + météo, SANS vanne ni
     min/max (sinon ça déborde). Comme le petit widget iOS. Mêmes ids partagés (WidgetRender). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="8dp"
    android:background="@drawable/widget_bg">

    <ImageView
        android:id="@+id/widget_nuage"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:scaleType="fitCenter"
        android:src="@drawable/nuage_taquin_radieux"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/widget_temp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#33414F"
        android:layout_marginTop="2dp"
        android:text="" />

    <TextView
        android:id="@+id/widget_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#33414F"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="" />
</LinearLayout>
